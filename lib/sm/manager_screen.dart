import 'package:flutter/material.dart';

class ManagerScreenPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Scaffold(
      appBar: new AppBar(
        title: new Text("Manager Dashboard"),
      ),
      body: new Center(
        child: new Text("Manager Dashboard"),
      ),
    );
  }
}
import 'package:flutter/material.dart';

class OpenCheckPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Scaffold(
      appBar: new AppBar(
        title: new Text("Open Check"),
      ),
      body: new Center(
        child: new Text("Open Check"),
      ),
    );
  }
}